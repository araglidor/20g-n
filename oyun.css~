body {
    /* Genel arka plan rengi, kenarlarda görünecek renk */
    background-color: #f0f0f0; 
    font-family: Arial, sans-serif;
    margin: 0; /* Tarayıcının varsayılan margin'lerini sıfırlıyoruz */
}

.container {
    width: 960px; 
    margin: 0 auto; 
    background-color: #ffffff; 
    padding: 20px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); 
}

/* İçerik alanları için ek stil */
header, footer {
    text-align: center;
    padding: 20px 0;
    border-bottom: 1px solid #ddd;
}

footer {
    border-top: 1px solid #ddd;
    border-bottom: none;
    margin-top: 20px;
    font-size: 0.8em;
}

main {
	text-align: center;
}